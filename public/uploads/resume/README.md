# Resume

Place the current PDF resume here. The default path in `content/profile.json` is:

```text
/uploads/resume/sum-sovath-resume.pdf
```

Keep the same file name to update the resume without changing JSON.
