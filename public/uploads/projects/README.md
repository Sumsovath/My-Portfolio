# Project Images

Place project screenshots here. Use JPG, PNG, or WebP files. A useful size is about 1000 by 640 pixels.

Reference an image in `content/projects.json` like this:

```json
"image": "/uploads/projects/my-project.jpg"
```

Leave `image` empty to show the website's clean image fallback.
